# 1. 流程总览

```
输入参数
   │
   ├─(可选) 由 Re 估算湍流强度 I = 0.16·Re^(-1/8)
   │
   ├─▶ 湍动能    k = 3/2·(|U|·I)²
   │        │
   │        ├─▶ 混合长度  L = 0.07·Lc
   │        │       │
   │        │       ├─▶ 耗散率 ε = Cmu^0.75·k^1.5 / L
   │        │       └─▶ 耗散频率 ω = √k / (Cmu^0.25·L)   [等价: ω = ε/(Cmu·k)]
   │        │
   │        └─▶ nuTilda 初始值（SA 模型前置，可选）
   │                nuTilda ≈ nut = Cmu·k²/ε（精确）
   │                或工程估算：3ν / 5ν / 10ν / ν·√(0.01·Re)
   │
   ├─▶ 湍流粘度（按所选模型取其一）
   │        ├─ k-ε 模型: nut = Cmu·k²/ε
   │        ├─ k-ω 模型: nut = k/ω
   │        └─ SA 模型:  nut = nuTilda·fv1
   │
   └─▶ 湍流粘度比  nutRatio = nut / ν
   │
   ▼
输出参数
```



# 2. 输入参数列表

| 符号 | 名称 | 单位 | 必选/可选 | 说明 |
| --- | --- | --- | --- | --- |
| \|U\| | 平均流速 | m/s | 必选 | 入口平均速度 |
| Lc | 特征长度 | m | 必选 | 如管道直径 D、水力直径、翼型弦长 |
| ν | 流体运动粘度 | m²/s | 必选 | 介质物性 |
| I | 湍流强度 | — | 可选 | 取值 0.01~0.1；自由来流常取 0.05；也可由 Re 估算 |
| Re | 雷诺数 | — | 可选 | 用于 I = 0.16·Re^(-1/8) 与 nuTilda = ν·√(0.01·Re) |
| Cmu | 模型系数 | — | 可选 | 通常取 0.09 |
| nuTilda | SA 模型输运量 | m²/s | SA 模型可选 | 不给定可按下文步骤 5 估算；也可直接取教程经验值 |
| Cv1 | SA 模型系数 | — | SA 模型必选 | 通常取 7.1 |



# 3. 计算步骤

## 步骤 0（可选）：估算湍流强度

已知雷诺数 Re 时（Fluent 管道充分发展流经验公式）：

$$
I = 0.16 \cdot \mathrm{Re}^{-1/8}
$$

> 若用户直接给定 I（如自由来流取 5%），跳过此步。

## 步骤 1：湍动能 k

$$
k = \frac{3}{2}(\left|\mathbf{U}\right| \cdot I)^2
$$

## 步骤 2：混合长度 L

$$
L = 0.07 \cdot L_c
$$

## 步骤 3：湍流耗散率 ε

$$
\varepsilon = \frac{C_\mu^{0.75} k^{3/2}}{L}
$$

## 步骤 4：湍流耗散频率 ω

基于混合长度理论：

$$
\omega = \frac{\sqrt{k}}{C_\mu^{0.25} L}
$$

等价形式（由 ε 换算，两者结果一致，可用于交叉验证）：

$$
\omega = \frac{\varepsilon}{C_\mu k}
$$

## 步骤 5：nuTilda 初始值（仅 SA 模型需要，可选）

OpenFOAM 源代码未直接提供 nuTilda 初始值公式，常用以下方法估算：

### 5.1 基于湍流粘度（较精确）

自由流区域 fv1 ≈ 1，因此 nuTilda ≈ nut：

$$
\tilde{\nu} = \nu_t = \frac{C_\mu k^2}{\varepsilon}
$$

> 需先完成步骤 1~3。

### 5.2 简化工程经验公式（最常用）

| 方法 | 公式 | 适用场景 |
| --- | --- | --- |
| 3ν | nuTilda = 3·ν | 内部流动（管道、通道等），最常用 |
| 5ν | nuTilda = 5·ν | 外部流动（翼型、车辆等） |
| 10ν | nuTilda = 10·ν | 高湍流情况 |

### 5.3 基于雷诺数

$$
\tilde{\nu} = \nu \cdot \sqrt{0.01 \cdot \mathrm{Re}}
$$

> 注意事项：
>
> 1. nuTilda 初始值只影响收敛初期的行为，通常会在若干时间步内调整到正确值；
> 2. 不确定时取 `nuTilda = 3·ν` 是安全的选择；
> 3. 壁面边界条件通常设置为 `nuTilda = 0`。

## 步骤 6：湍流粘度 nut（按所选湍流模型取其一）

### 6.1 k-ε 模型

$$
\nu_t = \frac{C_\mu k^2}{\varepsilon}
$$

### 6.2 k-ω 模型

$$
\nu_t = \frac{k}{\omega}
$$

### 6.3 Spalart-Allmaras 模型

$$
\nu_t = \tilde{\nu} \cdot f_{v1}
$$

其中阻尼函数：

$$
f_{v1} = \frac{\chi^3}{\chi^3 + C_{v1}^3}, \qquad \chi = \frac{\tilde{\nu}}{\nu}, \qquad C_{v1} = 7.1
$$

## 步骤 7：湍流粘度比

$$
\mathrm{nutRatio} = \frac{\nu_t}{\nu}
$$



# 4. 输出参数列表

| 符号 | 名称 | 单位 | 来源步骤 | 用途 |
| --- | --- | --- | --- | --- |
| I | 湍流强度 | — | 输入或步骤 0 | 边界条件、k 计算 |
| k | 湍动能 | m²/s² | 步骤 1 | k-ε / k-ω 入口条件 |
| L | 混合长度 | m | 步骤 2 | ε、ω 计算的中间量 |
| ε | 湍流耗散率 | m²/s³ | 步骤 3 | k-ε 入口条件 |
| ω | 湍流耗散频率 | 1/s | 步骤 4 | k-ω 入口条件 |
| nuTilda | 修正的湍流粘度 | m²/s | 步骤 5 | SA 入口/初始条件 |
| nut | 湍流粘度 | m²/s | 步骤 6 | nut 入口条件（随模型不同） |
| nutRatio | 湍流粘度比 | — | 步骤 7 | 边界条件校核（通常入口取 1~10） |

各模型典型输出组合：

| 湍流模型 | 需输出的入口参数 |
| --- | --- |
| k-ε | k、ε（或 I + ε / I + 混合长度） |
| k-ω / SST | k、ω |
| Spalart-Allmaras | nuTilda（由步骤 5 计算或直接给定） |



# 5. 数值算例

**输入：** |U| = 10 m/s，I = 5%，Lc = D = 0.5 m，ν = 1e-5 m²/s，Cmu = 0.09

| 步骤 | 计算 | 结果 |
| --- | --- | --- |
| 1 | k = 1.5 × (10 × 0.05)² | k = 0.375 m²/s² |
| 2 | L = 0.07 × 0.5 | L = 0.035 m |
| 3 | ε = 0.09^0.75 × 0.375^1.5 / 0.035 | ε ≈ 1.079 m²/s³ |
| 4 | ω = √0.375 / (0.09^0.25 × 0.035) | ω ≈ 31.95 1/s |
| 4' | ω = ε/(Cmu·k) = 1.079/(0.09×0.375) | ω ≈ 31.96 1/s ✓ |
| 5a | nuTilda ≈ nut = 0.09 × 0.375² / 1.079 | nuTilda ≈ 0.0117 m²/s |
| 5b | nuTilda = 3ν = 3 × 1e-5 | nuTilda = 3e-5 m²/s（保守） |
| 5c | Re = 10×0.5/1e-5 = 5e5；nuTilda = 1e-5 × √(0.01×5e5) | nuTilda ≈ 7.07e-4 m²/s |
| 6a | nut(k-ε) = 0.09 × 0.375² / 1.079 | nut ≈ 0.0117 m²/s |
| 6b | nut(k-ω) = 0.375 / 31.95 | nut ≈ 0.0117 m²/s ✓ |
| 6c | χ = 0.0117/1e-5 = 1170，fv1 ≈ 1；nut(SA) = 0.0117 × 1 | nut ≈ 0.0117 m²/s ✓ |
| 7 | nutRatio = 0.0117 / 1e-5 | nutRatio ≈ 1173 |

> 算例验证：
> - ε 与 ω 的两种换算公式结果一致，k-ε 与 k-ω 模型计算的 nut 一致；
> - 步骤 5a 的 nuTilda ≈ nut，代入 SA 模型后 fv1 ≈ 1，nut 结果与 k-ε / k-ω 一致，说明整套公式自洽；
> - 工程简化法（5b/5c）给出的 nuTilda 明显小于精确值（5a），属于保守初始化，收敛后会自动调整。



# 6. 反向校核（已知 k 求 I）

若已知 k 与 U，可反算湍流强度（与输入 I 相互校验）：

$$
I = \frac{\sqrt{2k/3}}{\max\left(\sqrt{2k/3},\ |\mathbf{U}|\right)}
$$

> 通常入口处 |U| 远大于 √(2k/3)，分母取 |U|，公式退化为 I = √(2k/3)/|U|。
